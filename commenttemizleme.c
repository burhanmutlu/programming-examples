#include <stdio.h>
#include <string.h>
int main()
{
	FILE *c_file = fopen("yorumsil.c", "w");
	char *kodlar = "";
	fread(c_file, 5, 5, )
	fprintf()
	fopen64("dene.txt", "w");
	fputs("merhaba", c_file);
	fclose(c_file);
	
}
